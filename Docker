docker build -t folderNmae .

docker image ls

docker login

docker image ls

docker tag do:latest username/firstIamge

docker images

docker push username/firstImage


FROM node:alpine
COPY . /app
WORKDIR /app
CMD node app.js   (Dockerfile)
